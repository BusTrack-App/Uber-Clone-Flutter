import 'package:flutter/material.dart';

class DriverMapTripContent extends StatelessWidget {
  const DriverMapTripContent({super.key});

  // DriverMapTripState state;
  // ClientRequestResponse? clientRequest;
  // TimeAndDistanceValues? timeAndDistanceValues;

  // DriverMapTripContent(
  //   this.state,
  //   this.clientRequest,
  //   this.timeAndDistanceValues, {
  //   super.key,
  // });

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('Driver Map Trip Content')));
  }




}
